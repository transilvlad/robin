/**
 * Establishes and manages the underlying SMTP connection.
 *
 * <p>Connection and SmtpFoundation work together to provide the core networking and containers mostly common to both server and client.
 * <br>The Connection class is responsible for the socket connection and I/O.
 * <br>The SmtpFoundation class is responsible for the SMTP session and state.
 */
package com.mimecast.robin.smtp.connection;
